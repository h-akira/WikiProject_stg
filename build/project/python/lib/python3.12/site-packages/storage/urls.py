from . import views
from had.urls import path

# app_name = "test"
  # path("logout", views.logout, name="logout", methods=["GET"])

urlpatterns = [
  path("{username}", views.post, name="post", methods=["POST"]),
  path("{username}/{name}", views.get, name="get", methods=["GET"])
]

