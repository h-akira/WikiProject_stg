from . import views
from had.urls import path

# app_name = "test"
  # path("logout", views.logout, name="logout", methods=["GET"])

urlpatterns = [
  path("", views.index, name="index", methods=["GET"]),
  path("create", views.create, name="create", methods=["GET", "POST"], login_required=True),
  # path("share/{share_code}", views.share_detail, name="share_detail", methods=["GET"]),
  # path("share/{share_code}/edit", views.share_edit, name="share_edit", methods=["GET", "POST"]),
  path("detail/{username}/{slug}", views.detail, name="detail", methods=["GET"])
  # path("update/{username}/{slug}", views.update, name="index", methods=["GET","POST"]),
  # path("delete/{username}/{slug}", views.delte, name="index", methods=["GET"]),
]

